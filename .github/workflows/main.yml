on:
  push:
    branches: [ main ]
    
jobs:
  deploy-prod:
    runs-on: self-hosted
    steps:
      - run: cd /root/clubManager
      - run: pwd
      - run: git pull
      - run: ./deploy/run.sh
    
    
